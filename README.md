# AI Mini EPS

Frontend-only React SPA for text generation, image previews, and saved results.

## What changed

- No backend
- No external API key
- No external network requests for generation
- Results are produced locally in the browser and saved with `localStorage`

## Features

- Home screen with navigation
- Text generator
- Image generator
- Saved items view
